{
  "verdict": "PASS — isolation proven; DB-dependent QA executed against an isolated, PII-free branch",
  "stopCondition": false,
  "resolvedAt": "2026-07-19",
  "howUnblocked": "Created a fresh Neon branch 'qa-synthetic-testing' (branch id br-plain-firefly-aqp4iqje) using 'Branch schema only (Beta)' — copies the schema with ZERO data rows, so NO cloned customer PII. Auto-deletes after 1 day. Connection string was placed into the worktree .env via clipboard→PowerShell without the password ever appearing in any log, screenshot, or output.",
  "isolationProof": {
    "endpoint": "ep-raspy-term-aqwjf17b (QA branch) — DISTINCT from production ep-sweet-haze-aq6nra0j and from the stale ep-crimson-sea in the pre-existing .env",
    "productionEndpointGuard": "Every QA script hard-aborts if DATABASE_URL contains 'ep-sweet-haze' (the production endpoint).",
    "rowCountsAtProvision": "Customer/Branch/User/CustomerEdit/AuditLog/Attachment/Region/Route ALL 0 rows — schema-only branch carries no PII.",
    "schemaPresent": "18 public tables copied from production's live schema.",
    "migrationFingerprintAtProvision": "_prisma_migrations empty (schema-only copy omits history rows); tables physically present."
  },
  "provisioning": {
    "method": "prisma db push (sync schema.prisma) + a manual idempotent DDL patch to restore migration-only invariants db push cannot express.",
    "dbPushFinding": "prisma db push DROPPED migration-only partial-unique indexes + CHECK constraints (CustomerEdit_open_per_branch and 7 Phase-1 CHECKs). This is a QA-provisioning artifact, NOT a production risk: the deploy path is `prisma migrate deploy` (package.json db:deploy), which runs the raw migration SQL. OPERATIONAL RULE: never provision/deploy this schema with `db push`.",
    "invariantsRestored": "CustomerEdit_open_per_branch, EditBranchDraft gps/address CHECKs + region-consistency trigger, Customer/CustomerEdit credit CHECKs — verified present post-patch (constraint smoke: 12 CHECKs, 2 expected partial-uniques).",
    "expectedAbsent": "Customer_primaryPhoneNorm_active_unique is INTENTIONALLY absent — migration 20260510160000_p1_drop_phone_unique drops it (phone dups allowed at master level)."
  },
  "checks": {
    "emailIsolation": "PASS — no mailer/provider wired; in-app Notification rows only.",
    "r2Isolation": "DEFERRED — R2_BUCKET=nmwc-photos is the PRODUCTION bucket. The customer-master IMPORT path reads the xlsx from an in-memory buffer and stages to DB (importBatch/importRow) with NO R2 write, so import QA is fully isolated. Photo/attachment write-path tests remain deferred/mocked.",
    "sentryIsolation": "PASS — server Sentry DSN disabled (#DEMO-OFF).",
    "dbIsolation": "PASS — isolated empty schema-only branch, endpoint-guarded, auto-expiring."
  },
  "writesPerformed": "Only self-created synthetic rows (prefixes ZZ-REACT-, ZZ-IMP-, ZZDIRTY-/ZZDUP-/ZZPH-/ZZXW-/ZZGPS), each deleted in the test afterAll. No production writes. No shared/parent role password reset."
}
