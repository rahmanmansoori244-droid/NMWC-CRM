# Changelog

## [Unreleased]

### M0 — Foundation
- Initial Next.js 15 + TypeScript + Tailwind scaffold
- Brand: NMWC wordmark + blue palette
- Login page (UI only — auth wired in M1)
- `/api/health` endpoint
- Vitest + Playwright configured
- Pino structured logger with PII redaction
- Standard error taxonomy (`AppError`, `ValidationError`, etc.)
- ESLint + Prettier configured
- GitHub Actions CI: typecheck + lint + tests
- `.env.example` documenting required env vars
